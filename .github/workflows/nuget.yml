name: Nuget Push

on:
  push:
    branches:
      - develop

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.0.101

    - name: Build with dotnet
      run: |
        cd Chakra.Core.Mocks
        dotnet build --configuration Release

    - name: Package Chakra.Core.Mocks
      run: |  
        cd Chakra.Core.Mocks
        dotnet pack -c Release -o out

    - name: Publish Chakra.Core.Mocks to Nuget
      run: |
        cd Chakra.Core.Mocks
        dotnet nuget push ./out/$(ls ./out) -s https://api.nuget.org/v3/index.json -k oy2c24gaod2kmcwlviwqbpv4wbjbrqlenf45pgpjglm37q
      env:
        APIKEY: ${{ secrets.NUGET_APIKEY }} 
